# DONE

- [x] Build local funcional do kernel Nethunter (`4.19.113-Nethunter_WirusMOD_r8q_v4.1`): resolvidos deps do host (bison/flex/libelf-dev), submodules CAN, e o fix do `-Wunused-function` que o `gcc-wrapper.py` da QCOM tornava fatal — 2026-08-06 (commit cffff32f1)
- [x] `build.sh` endurecido (set -euo pipefail, saída dentro do repo, remoção dos export inertes LLVM/CC/LD) — 2026-08-06 (commit 5b3c23d09)
- [x] Packaging separado: zip **kernel-only** (não toca partição read-only) + **módulo Magisk companion** (.ko/hid/init via overlay.d+magic mount) — 2026-08-06 (commit d61e3ce26)
- [x] `docs/diff-stock-vs-nethunter.md`: diff de config stock (via `/proc/config.gz` por SSH) vs Nethunter, categorizado — 2026-08-06 (commit 9affaf518)
- [x] Comparação dos 3 kernels (stock / Nethunter / not_samsung 4.19.325+KSU) e do driver qcacld (o `not` já tem monitor mode no source; porte desnecessário) — 2026-08-06
- [x] Diagnóstico completo do monitor mode no wlan0 interno (QCA6390): entra em monitor (radiotap) mas fixar canal deadlocka o driver → inviável para captura; dongle USB é o caminho — 2026-08-06 (ver memória do projeto)
- [x] **Correção do BadBT (HIDP/BNEP):** o commit `aa54a27eb` descomentou as linhas no `net/bluetooth/Makefile` mas as opções seguiam `is not set` no defconfig → nada compilava. Habilitado `BT_HIDP`/`BT_BNEP` (+ VLAN 802.1Q, que destravou o `BRIDGE_VLAN_FILTERING` já presente porém descartado). Validado no device: com BT ligado, `/proc/net/protocols` lista HIDP/BNEP/RFCOMM/L2CAP — 2026-08-07 (commit fad6f44d0)
- [x] **Limpeza do defconfig:** removidos 17 símbolos que nenhum Kconfig da árvore define (`ZFS`, `AUFS_FS` — quem cobre o chroot é `OVERLAY_FS`, `CIFS_SMB2`, 12× `KEYSPAN_*`…), `BLK_DEV_LOOP_MIN_COUNT` de volta a 48 (stock), `MODULE_FORCE_UNLOAD`/`BINFMT_MISC` on, sintaxe `=n` → `# is not set` — 2026-08-07 (commit fad6f44d0)
- [x] **`build.sh`: strip dos `.ko`** (`--strip-debug`; can327.ko 495K→26K) e nota sobre ccache incompatível com `-g` — 2026-08-07 (commit f0f16d162)
- [x] **Modularização dos drivers de dongle** (30 drivers realtek OOT/ath/rt2x00/mt76/zd1211rw/… `=y`→`=m`): ~7 MB de kernel residente a menos; `cfg80211`/`mac80211` seguem built-in. `DEBUG_INFO` desligado (vmlinux 690→61 MB, destrava ccache; build incremental 17min→1min). Renomeado para `NetHunter_matheuslive_r8q_v4.2`. Companion agora roda `depmod` e traz o helper `usbwifi` — 2026-08-07 (commit 34e4972d3)
- [x] **Fix `rtl8188eus`: parava de consumir símbolos do `rtl8812au`** (`rtw_change_ifname`/`NULL_hdl`) — só linkava built-in por resolução cruzada no `vmlinux` (executava código de outro fork sobre `_adapter` de layout diferente); MODPOST acusou ao virar `=m` — 2026-08-07 (commit 26ae8c1c0)
- [x] **Firmwares dos dongles no companion** (`system/vendor/firmware`, 25 arquivos/580 KB): via fallback do user helper → `/vendor/firmware` (nenhum driver usa `request_firmware_direct`). Descoberto que o AR9271 nunca teria funcionado no template — driver pede `ath9k_htc/htc_9271-1.4.0.fw`, template tinha `htc_9271.fw` solto (mesmos bytes, nome/path errados) — 2026-08-07 (commit 2378105f8)
- [x] **Descartado o porte de stable 4.19.113→325 para esta árvore:** medido — 17.405 commits, 24% dos hunks só aplicam com fuzz em `ext4`/`dwc3`/`conntrack`. Fuzz em patch de segurança = árvore Frankenstein não-bootável. Rota realista continua sendo o `not_kernel` (que já boota o source ACK), não patch cru aqui — 2026-08-07
- [x] **Flash validado:** v4.2 bootou, root Magisk preservado (AnyKernel faz hexpatch `skip_initramfs`, boot.img v2, ramdisk intacto). Auditado o que o zip kernel-only realmente faz — e que o **DTB do repo NÃO chega ao device** (boot v2 usa a seção `dtb` stock, que o AnyKernel só faz `magiskboot dtb patch`; `dtb.img`/`dtbo.img` gerados são órfãos, só úteis por fastboot/Odin) — 2026-08-07
