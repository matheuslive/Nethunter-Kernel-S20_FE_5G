# DONE

- [x] Build local funcional do kernel Nethunter (`4.19.113-Nethunter_WirusMOD_r8q_v4.1`): resolvidos deps do host (bison/flex/libelf-dev), submodules CAN, e o fix do `-Wunused-function` que o `gcc-wrapper.py` da QCOM tornava fatal — 2026-08-06 (commit cffff32f1)
- [x] `build.sh` endurecido (set -euo pipefail, saída dentro do repo, remoção dos export inertes LLVM/CC/LD) — 2026-08-06 (commit 5b3c23d09)
- [x] Packaging separado: zip **kernel-only** (não toca partição read-only) + **módulo Magisk companion** (.ko/hid/init via overlay.d+magic mount) — 2026-08-06 (commit d61e3ce26)
- [x] `docs/diff-stock-vs-nethunter.md`: diff de config stock (via `/proc/config.gz` por SSH) vs Nethunter, categorizado — 2026-08-06 (commit 9affaf518)
- [x] Comparação dos 3 kernels (stock / Nethunter / not_samsung 4.19.325+KSU) e do driver qcacld (o `not` já tem monitor mode no source; porte desnecessário) — 2026-08-06
- [x] Diagnóstico completo do monitor mode no wlan0 interno (QCA6390): entra em monitor (radiotap) mas fixar canal deadlocka o driver → inviável para captura; dongle USB é o caminho — 2026-08-06 (ver memória do projeto)
- [x] **Correção do BadBT (HIDP/BNEP):** o commit `aa54a27eb` descomentou as linhas no `net/bluetooth/Makefile` mas as opções seguiam `is not set` no defconfig → nada compilava. Habilitado `BT_HIDP`/`BT_BNEP` (+ VLAN 802.1Q, que destravou o `BRIDGE_VLAN_FILTERING` já presente porém descartado). Validado no device: com BT ligado, `/proc/net/protocols` lista HIDP/BNEP/RFCOMM/L2CAP — 2026-08-07 (commit fad6f44d0)
- [x] **Limpeza do defconfig:** removidos 17 símbolos que nenhum Kconfig da árvore define (`ZFS`, `AUFS_FS` — quem cobre o chroot é `OVERLAY_FS`, `CIFS_SMB2`, 12× `KEYSPAN_*`…), `BLK_DEV_LOOP_MIN_COUNT` de volta a 48 (stock), `MODULE_FORCE_UNLOAD`/`BINFMT_MISC` on, sintaxe `=n` → `# is not set` — 2026-08-07 (commit fad6f44d0)
- [x] **`build.sh`: strip dos `.ko`** (`--strip-debug`; can327.ko 495K→26K) e nota sobre ccache incompatível com `-g` — 2026-08-07 (commit f0f16d162)
- [x] **Modularização dos drivers de dongle** (30 drivers realtek OOT/ath/rt2x00/mt76/zd1211rw/… `=y`→`=m`): ~7 MB de kernel residente a menos; `cfg80211`/`mac80211` seguem built-in. `DEBUG_INFO` desligado (vmlinux 690→61 MB, destrava ccache; build incremental 17min→1min). Renomeado para `NetHunter_matheuslive_r8q_v4.2`. Companion agora roda `depmod` e traz o helper `usbwifi` — 2026-08-07 (commit 34e4972d3)
- [x] **Fix `rtl8188eus`: parava de consumir símbolos do `rtl8812au`** (`rtw_change_ifname`/`NULL_hdl`) — só linkava built-in por resolução cruzada no `vmlinux` (executava código de outro fork sobre `_adapter` de layout diferente); MODPOST acusou ao virar `=m` — 2026-08-07 (commit 26ae8c1c0)
- [x] **Firmwares dos dongles no companion** (`system/vendor/firmware`, 25 arquivos/580 KB): via fallback do user helper → `/vendor/firmware` (nenhum driver usa `request_firmware_direct`). Descoberto que o AR9271 nunca teria funcionado no template — driver pede `ath9k_htc/htc_9271-1.4.0.fw`, template tinha `htc_9271.fw` solto (mesmos bytes, nome/path errados) — 2026-08-07 (commit 2378105f8)
- [x] **Descartado o porte de stable 4.19.113→325 para esta árvore:** medido — 17.405 commits, 24% dos hunks só aplicam com fuzz em `ext4`/`dwc3`/`conntrack`. Fuzz em patch de segurança = árvore Frankenstein não-bootável. Rota realista continua sendo o `not_kernel` (que já boota o source ACK), não patch cru aqui — 2026-08-07
- [x] **Flash validado:** v4.2 bootou, root Magisk preservado (AnyKernel faz hexpatch `skip_initramfs`, boot.img v2, ramdisk intacto). Auditado o que o zip kernel-only realmente faz — e que o **DTB do repo NÃO chega ao device** (boot v2 usa a seção `dtb` stock, que o AnyKernel só faz `magiskboot dtb patch`; `dtb.img`/`dtbo.img` gerados são órfãos, só úteis por fastboot/Odin) — 2026-08-07
- [x] **Kernel v4.3 + módulo Magisk de bateria** — `CONFIG_ENG_BATTERY_CONCEPT=y` (destrava os ~20 `batt_tune_*` no sysfs, confirmado no objeto compilado e no aparelho) e `OTG_ILIM` 900→1500 mA no `max77705_charger.c`. Módulo novo `packaging/magisk-battery/` → `nethunter-battery-r8q.zip`: CLI `battctl` (22 parâmetros + 3 chaves de módulo), aplicação no boot, config em `/data/adb/nethunter-battery.conf` com seção própria para o configurado — 2026-08-08
- [x] **`build.sh` publica e poda sozinho** — os três zips vão para a raiz do repo (symlink) e para a raiz do SD externo do S20 (`scp`, mount descoberto pelo UUID), e em ambos fica só a build mais recente de cada zip — 2026-08-08
- [x] **v4.5: `batt_age_step` — patch em C que expõe o degrau de age forecast** — 2026-08-08. O que rebaixa a tensão máxima conforme os ciclos é o `sec_bat_aging_check()`, que escolhe o degrau na tabela `battery,age_data` do DTS e chama `sec_bat_set_aging_step()` (rebaixa float, `swelling_normal_float_voltage`, recarga e condição de cheio de uma vez). **Não havia sysfs para desligá-lo nem para fixar o degrau** — só `battery_cycle`/`battery_cycle_test` —, então não dava para fazer pelo módulo Magisk. Patch: `BATT_AGE_STEP` no enum + `SEC_BATTERY_ATTR(batt_age_step)` (mesma posição relativa nos dois, senão saem de sincronia), `show` devolvendo só o degrau (para a releitura do `wr` do battctl ecoar o escrito) e `store` chamando a função direto, **retornando `-EINVAL`** em step inválido — é o único atributo deste sysfs em que o código de retorno do write vale. Exposto como chave `AGE_STEP` no `battctl`, aplicada depois do `BATTERY_CYCLE` (vence se ambos estiverem no conf). **Armadilha:** `sec_bat_set_aging_step()` é `__visible_for_testing`, que expande para `static` sem `CONFIG_KUNIT` (o `#define` vazio na linha 33 do `sec_battery.c` está dentro de um `#if defined(CONFIG_KUNIT)`) — declarar `extern` no header quebra o build com "static declaration follows non-static declaration"; resolvido com o wrapper público `sec_bat_force_aging_step()` no próprio `sec_battery.c`
- [x] **nftables da v4.4 validado no aparelho pelo `iptables-nft`** — 2026-08-08. O Termux **não empacota `nftables`** (nem o TUR): só `libnftnl` 1.3.1. O caminho que serve é o `iptables` 1.8.13, que traz o `iptables-nft` — justamente o consumidor do `NFT_COMPAT` e o backend padrão do Kali. Teste: `iptables-nft --version` reporta `(nf_tables)`, e criar chain isolada + regra (`-N CLAUDE_TEST`, `-A ... -p tcp --dport 12345 -j DROP`) funcionou com leitura de volta e contadores; `ip6tables-nft -L` responde, confirmando a família ip6. Chain removida depois, sem resíduo. O warning `iptables-legacy tables present` é esperado: o `netd` do Android usa o backend legacy, que fica separado. Para o `nft` puro sobram duas rotas, nenhuma imediata: compilar no Termux (deps todas no repo — `libmnl`, `libnftnl`, `libedit`, `readline`, `bison`, `flex`, `clang`; o `gmp` que falta não é mais dependência do nftables desde a 0.9) ou o chroot em `/data/local/nhsystem/kalifs`, que existe mas está desmontado e sem o app `com.offsec.nethunter` instalado
- [x] **Pós-boot da v4.4 validado no aparelho** — 2026-08-08. O swap do `modules_update` aconteceu (o diretório sumiu) e o magic mount publicou os três binários em `/system/bin` (`battctl`, `usbwifi`, `hid-keyboard`), os 36 `.ko` em `/system/lib/modules` e os firmwares em `/vendor/firmware/ath9k_htc/`. Bateria: `siop_level` 444, 20 `batt_tune_*`, e o `service.sh` registrou no log a aplicação de `FULL_CAPACITY=0` / `FLOAT_VOLTAGE=4270` / `SIOP_LEVEL=100`. Features novas vivas: `cp210x` em `/sys/bus/usb-serial/drivers/`, squashfs em `/proc/filesystems`, 100 símbolos `nf_tables` e `register_kprobe` no `kallsyms`. **Buffer de log: 9.695 → 37.545 linhas retidas (3,9×, bate com o 4× calculado).** Sem panic no `last_kmsg`; os únicos erros do boot são `binder: transaction failed` (rotina) e `vaultkeeper: Error into TA side (-10029/KnoxGuard)`, consequência esperada do KNOX desligado no defconfig. Isto fecha o item "reiniciar e conferir o pós-boot dos dois módulos"
- [x] **Auditoria de estado (kernel v4.3 no aparelho) + v4.4 de defconfig** — 2026-08-08. Aparelho saudável: sem panic/Oops (as "32 ocorrências" no `last_kmsg` eram `DEBUG:` do AVB casando com o grep de `BUG:`), SELinux Enforcing sem denial nosso, HIDP/BNEP/RFCOMM/L2CAP e VLAN vivos, 20 `batt_tune_*` no sysfs, 36 `.ko` com `modules.dep`, firmware `ath9k_htc` com os nomes certos. **Achado principal:** o ring buffer do kernel rodava inteiro em **85 s** (9695 linhas, 114 linhas/s) — causa é userspace, o `ChargingNotificationService` do app `com.matheus.charger` varrendo `/sys/class/power_supply/battery/` a 1,6 Hz (cada varredura dispara os `pr_info` do `sec_bat_show_attrs` + i2c ao MFC, que responde `-6` sem carregador wireless). Os `-6`/`-22` do `dmesg` são benignos: driver perguntando por hardware ausente
- [x] **v4.4: `LOG_BUF_SHIFT` 17→22 e quatro lacunas de config** — 2026-08-08. O buffer efetivo **já era 1 MB, não 128 KB**: `log_buf_add_cpu()` soma `(NR_CPUS-1) × LOG_CPU_MAX_BUF_LEN` = 7 × 128 KB quando isso passa de metade do `__LOG_BUF_LEN`; por isso 17→20 daria só 1,8×. Com 22 o `cpu_extra` (896 KB) deixa de contribuir (≤ 2 MB) e o buffer fica em 4 MB fixos = 4× o de hoje, ~5,5 min de histórico, +3 MB de RAM. Junto entraram `USB_SERIAL_CP210X=y` (CP2102 por OTG — CH341/FTDI/PL2303 já estavam, só ele faltava), `KPROBES=y`, **nftables** (36 símbolos, com `NFT_COMPAT` que é o que o `iptables-nft` do Kali usa) e **squashfs** (XZ/ZSTD/LZ4/LZO/xattr). Validado no `System.map` do build: `nf_tables_module_init`, `nft_compat_module_init`, `cp210x_*`, `squashfs_*`, `register_kprobe`. Kernel 22,09 → 22,22 MB
- [x] **Fix do caminho dos binários nos dois módulos Magisk** — `system/xbin` nunca foi montado pelo Magisk 30700 (o diretório nem existe em A13), então `battctl`, `hid-keyboard` e `usbwifi` jamais chegaram ao PATH; movidos para `system/bin`. Os instaladores passaram a publicar tudo sem exigir reboot (`/debug_ramdisk` + overlayfs para o firmware) — 2026-08-08

- [x] **v4.6 — primeira onda de otimização de velocidade/bateria**, validada no aparelho. `DEBUG_QSPINLOCK_OWNER` off (o `arch_spinlock_t` voltou de 16 para 4 bytes; todo `spinlock_t` do kernel usa esse tipo, então `task_struct`/`runqueue`/`inode`/`dentry` encolheram junto), `SEC_DEBUG_SCHED_LOG` off (gravava ~7 campos + leitura de clock a cada context switch e em 4 pontos de IRQ), `DEFAULT_IOSCHED` cfq→deadline, `DEFAULT_TCP_CONG` bic→cubic, `CPU_FREQ_STAT=y` (sem ele o `time_in_state` vinha vazio e não havia como medir). Confirmado no boot: `[deadline]` no sda, `cubic`, `time_in_state` respondendo em 17/18/20 degraus, `/proc/last_kmsg` preservado, 33 módulos com vermagic v4.6, zero panic/call trace no dmesg — 2026-08-10 (commits 598fe56ed, 80632f772)
- [x] **Bug latente do source Samsung exposto pela v4.6:** `SEC_DEBUG_SUMMARY` tem `depends on SEC_DEBUG_SCHED_LOG` e caiu junto, mas `sec_set_mdm_summary_info()` — definida em `sec_debug_summary.c` — é chamada em `mhi_pm.c:264,1088` sob `SEC_DEBUG_MDM_FILE_INFO`, símbolo sem dependência declarada entre os dois → link quebrado. Resolvido desligando o `MDM_FILE_INFO` (é `default n` e `(DEPRECATED)`, só alimenta a tela de UPLOAD mode, que não roda aqui). Os outros 6 símbolos globais do arquivo referenciados de fora têm stub correto — 2026-08-10 (ver memória do projeto)
- [x] **Armadilha de build descoberta:** diretório vazio homônimo ao defconfig (`wirus_defconfig/`) na raiz do repo faz o `make` pular a geração do `.config` **em silêncio** e buildar com a config antiga — custou dois ciclos de build. O tell no log é a ausência de `configuration written to .config`. Validar sempre por `out/.config`, nunca pelo defconfig editado — 2026-08-10 (ver memória do projeto)
- [x] **`AGE_STEP` da v4.5 validado no aparelho** (o recurso existia desde a v4.5 mas nunca tinha rodado): `get` devolveu `4` com float em 4270 mV; `set 0` levou a float a **4380 mV** e o dmesg registrou `BATT_AGE_STEP(0/4) float(4380mV)` — o `0/4` confirma `num_age_step=5` como o DTS indica; step inválido (`7`) foi **rejeitado**, diferente do resto deste sysfs. Revertido para `4` ao fim do teste (aparelho estava descarregando, então a float não teve efeito prático) — 2026-08-10
- [x] **Baseline da v4.4 capturada e versionada** em `docs/baseline/` antes do flash, com README explicando as duas janelas (contadores do kernel = 46 h de uptime; batterystats = 3 h 22 min desde a última carga) — 2026-08-10 (commit 80632f772)
- [x] **v4.7 — segunda onda no defconfig.** `TRANSPARENT_HUGEPAGE_ALWAYS`→`MADVISE` (o item com evidência mais forte: medido na v4.6, o THP estava em `[always]` e **nunca** alocava — `thp_fault_alloc=0`, `AnonHugePages=0 kB`, 231.983 fallbacks; tentava em todo fault anônimo e falhava sempre, porque a memória vive fragmentada) + `IOMMU_DEBUG`, `QCOM_GPI_DMA_DEBUG`, `USB_DEBUG_DETAILED_LOG`, `SEC_DEBUG_APPS_CLK_LOGGING` e `SEC_DEBUG_FPUT_WATCHDOG` off. Build passou **de primeira** — a verificação prévia de dependências evitou os ciclos perdidos da v4.6 — 2026-08-11 (commit a9f698891)
- [x] **Três itens do plano da segunda onda descartados na verificação** (o motivo vale mais que a mudança): `SCHED_DEBUG=n` quebraria as escritas de `sched_tunable_scaling`/`sched_latency_ns`/`sched_wakeup_granularity_ns` do próprio `init.rc` do AOSP (linhas 336-338), que estão sob o `#ifdef` em `kernel/sysctl.c`, e tiraria três recursos do perf HAL da QCOM; `SCHEDSTATS=n` quebraria o `atrace.rc` e o `llkd`; `SEC_DEBUG_TSP_LOG=n` quebraria o **link** (`sec_debug_tsp_command_history()` chamada sem guarda em `sec_cmd.c:339`, com o header só incluído sob o `#ifdef` — mesmo padrão do `sec_set_mdm_summary_info` da v4.6) — 2026-08-11
- [x] **Piso de frequência do little: culpado identificado.** É o `/vendor/bin/init.qcom.post_boot.sh`, bloco `"kona")`, linha 5233. Não é restrição de hardware — escrever 300000 funciona e persiste. Achado colateral que mudou o plano: **todos** os tunables de schedutil (`up`/`down_rate_limit_us`, `hispeed_freq`, `pl`) saem desse mesmo bloco, conferidos um a um contra o runtime, logo não são ajustáveis por defconfig. Testado e **revertido** a pedido; decisão de não mexer — 2026-08-11 (commits b00152595, c8e1ae045)
- [x] **v4.7 validada no aparelho.** `enabled: always [madvise] never` e, o número que fecha o caso do THP: **`thp_fault_fallback = 0`** em 27 min de uptime, contra 231.983 acumulados na v4.6 (~257/min, o que daria ~6.900 no mesmo intervalo). Zerou sem perder nada, já que `thp_fault_alloc` sempre foi 0. Resto limpo: 33 módulos com vermagic v4.7, **zero** panic/oops/call trace, touchscreen logando normal via `[sec_input]` com `Cal_flag:Success` (o `SEC_DEBUG_TSP_LOG` foi preservado de propósito), USB sem erro, 11 input devices, e `[deadline]`/`cubic`/`time_in_state` preservados da v4.6 — 2026-08-11
- [x] **ThinLTO testado e descartado com dados.** Builda (exit 0) depois de vencer quatro obstáculos, mas o **`text` fica +0,2%** — o LTO praticamente não mexeu no código; residente +0,7%, `Image.gz-dtb` +4,4%. Custo: `ld.lld` no lugar do GNU ld (linker do kernel inteiro), `MODVERSIONS` off (ABI), `-j3` obrigatório e build muito mais lento. Ganho jamais verificado — tamanho não mede velocidade, então a decisão foi **não adotar** e manter a v4.7. Descobertas do caminho: (a) `arch/arm64/Kconfig:116` faz `select HAVE_ARCH_PREL32_RELOCATIONS if !LTO_CLANG`, o que torna os `__crc_*` absolutos e faz o lld rejeitá-los; (b) o `genksyms` **falha desde sempre** com `union __packed` por valor — ≥5 símbolos no `gsi.c` —, warning inofensivo sem LTO e erro fatal com ele; (c) os crashes do clang em arquivos aleatórios **não eram bug do compilador** (cada um compila 3/3 isolado), e sim pressão de memória, com ~12 dos 23 GB presos nas VMs do PVE. Build apagado, árvore limpa, `wirus_defconfig` intocado — 2026-08-12
- [x] **Dreno de standby diagnosticado — em duas rodadas, porque as duas primeiras conclusões estavam erradas.** Resultado correto: (1) o **Bluetooth** custa ~42% do uptime (`hal_bluetooth_lock` 14,86 h + `qup_uart` 14,98 h de 35,1 h) e a causa é a **Mi Band** por BLE/GATT, mantida pelo `com.mc.miband1` (cliente GATT `appIf: 6`, `NotifyService` ativo) — não é "BT ligado à toa"; (2) o **NFC** segura o wakelock **~98% do tempo em que está ligado** (47 min de wakelock nos 48 min ligados após o boot; `disable()` às 08-11 13:18). Consumo de tela apagada: 63% em 8h59m ≈ 7%/h. Três lições de método: `active_since` é foto do instante e não taxa; `total_time` só vale normalizado pelo tempo em que o subsistema esteve **ligado**, não pelo uptime; e `ConnectionState: STATE_DISCONNECTED` fala dos perfis **clássicos** (A2DP/HSP) e nada diz sobre BLE — para BLE, olhar `GATT Client Map` — [[dreno-de-standby-nfc-e-mi-band]] — 2026-08-12
- [x] **Falhas de suspend reclassificadas para baixa prioridade.** O `last_failed_dev` é o `0000:01:00.0` = **`cnss_pci`** (WLAN QCA6390) com errno −16 (EBUSY): contenção normal com WiFi ativo, não defeito — o dmesg mostra `cnss: Runtime suspend`/`Runtime resume` operando certo. O padrão que de fato importa é outro: o device **suspende e acorda em 0,25–0,33 s**, e `[timerfd]` tem `wakeup_count` **12276**, idêntico ao número de suspends bem-sucedidos — todo suspend é encerrado por timer de userspace. Quem acorda: `com.google.android.gms` 1217 wakeups (719 de `ALARM_WAKEUP_ACTIVITY_DETECTION`) e `aodservice` 1092 — 2026-08-12
- [x] **v4.8 — `wmon`: monitor mode + airodump-ng no companion** — 2026-08-13 (commits d99f8938d, c808a7186). Script `packaging/magisk/system/bin/wmon`, roda sob demanda no Termux como root. **Autodetecta a interface** (pedido do usuário): prefere o dongle USB `wlan1` (caminho estável — `iw set monitor`, hopping/injection livres, restaura para `managed` ao sair) e cai no `wlan0` interno só como fallback (via `con_mode=4`). No interno impõe as travas que a memória do projeto exige: **canal fixo obrigatório** (`-c`, pois o hopping deadlocka o qcacld em D-state → hard reboot), exige `wlan0` já `operstate=up` antes do `con_mode=4` (o `ip link up` em monitor trava) e restaura com `con_mode=0` + `svc wifi disable/enable` via `trap`. Resolve `iw`/`airodump-ng` no PATH e, como fallback, no prefixo do Termux + chroot, setando `LD_LIBRARY_PATH` quando o binário é do Termux (sudo zera o PATH). Integrado ao `customize.sh` (permissão + publish sem-reboot no `/debug_ramdisk`) e ao README. Bump para v4.8 mantém o vermagic dos `.ko` em sincronia. Build limpo (exit 0), 33 `.ko`, os três zips gerados e **entregues no SD do S20 e na raiz do repo** (v4.7 podada dos dois lados); `wmon` confirmado dentro do companion zip e `module.prop` com `version=v4.8`

- [x] **v4.8.2 — monitor mode interno da QCA6390: causa-raiz + destravamento (Fases 0+1)** — 2026-08-16 (branch `monitor-qca6390-fix` merjado por fast-forward na `nethunter-13.0`, HEAD 04422d75b, pushado). Investigação a fundo confirmou **na fonte** que o deadlock **não é do nosso código**: `iw set channel` no monitor (`__wlan_hdd_cfg80211_set_mon_ch`, sob `rtnl_lock`) → `sme_roam_channel_change_req` → **VDEV_RESTART** que o firmware da QCA6390 nunca responde → `target_if_vdev_mgr_rsp_timer_cb` → `qdf_trigger_self_recovery(VDEV_START_RESPONSE_TIMED_OUT)` → SSR trava → hard reboot (o próprio driver documenta em `wlan_hdd_main.c:8026`). Patches: **0-A** early-return de mesmo canal (mata o freeze do airodump); **0-B** pular o self-recovery no timeout de vdev **monitor** (`target_if_vdev_mgr_rx_ops.c`, checando opmode) → converte o hard-reboot em erro recuperável; **1-C** pular a transição DBS no monitor. Fase 2-E (PE session de monitor) **já estava in-tree** (`lim_mon_init_session`); Fase 1-D (forçar START) descartada. **Validado no aparelho:** entra em monitor (até de `wlan0` DOWN) e o `iw set channel` agora **falha limpo com `-110` em vez de travar** (sem `reboot -f`), mas a captura em canal arbitrário é **impossível — muro de firmware confirmado** (o FW não emite VDEV_UP; alinha com o ath11k mainline ter desabilitado monitor na mesma QCA6390). Detalhes em [[monitor-mode-wlan0]]
- [x] **wmon: flag `-t`, endurecimento do caminho interno e default `wlan1`; `service.sh` carrega ath9k_htc no boot** — 2026-08-16. `wmon -t` = self-test do monitor interno (roda a sequência que travava com `timeout`, conta frames por `rx_packets`, restaura e dá veredito). O `wmon -c` interno passou a **partir de `wlan0` DOWN** antes do `con_mode=4` — evita o **vazamento de refcount de netdev** (`unregister_netdevice: waiting for wlan0 to become free`, stack `hdd_driver_mode_change→hdd_close_all_adapters→unregister_netdev`) que travava a troca de modo quando a STA estava ativa (custou um reboot no teste). E o **default do `wmon` virou sempre `wlan1`** (sem fallback pro interno; `wlan0` só com `-i wlan0` ou `-t`). Novo `service.sh` (Magisk late_start) carrega o `ath9k_htc` no boot via `usbwifi`, pro AR9271 virar `wlan1` no hotplug sem rodar `usbwifi` na mão. Tudo empacotado na v4.8.2, entregue no SD, commitado e pushado
- [x] **Infra de dongle USB validada no aparelho (o que faltava do item de monitor mode)** — 2026-08-16. Confirmado com dois dongles plugados: **AR9271** (`0cf3:9271`, ath9k_htc) — driver carrega e firmware transfere, mas falha a init de HW por energia (virou item no TODO); **RTL8188SU** (`0bda:8171`) — **sem driver** nesta árvore. A infra (companion, `usbwifi` casando VID:PID, firmwares em `/vendor/firmware`) funciona; o que resta é hardware (energia do OTG) e o boot do `service.sh`, agora em itens próprios no TODO. Matriz de chips suportados e o quirk do AR9271 em [[dongles-usb-suportados]]
