# TODO

- [ ] **Usar dongle USB WiFi para monitor mode** — infraestrutura pronta (2026-08-07): drivers `=m`, firmwares em `packaging/firmware`, helper `usbwifi` (casa VID:PID→módulo) e `depmod` no companion. Falta apenas: (1) ~~instalar o companion~~ **feito em 2026-08-08** (v4.3, já com o fix de `system/bin`); (2) ter o adaptador físico (AR9271/ath9k_htc recomendado, RTL8812AU tb); (3) preparar helper de monitor+injection para o `wlan1`. Fluxo depois de plugar: `usbwifi` carrega o driver certo (adicionado 2026-08-06, atualizado 2026-08-07)
- [ ] **Apagar o `~/moncap` do device** (`/data/data/com.termux/files/home/moncap`) — o monitor mode no rádio interno trava o driver em D-state (hard reboot); o helper não deve ser usado (adicionado 2026-08-06)
- [ ] **Base do kernel — decisão parcial (2026-08-07):** fica no Nethunter 4.19.113 por ora (boota, em uso). Porte de stable 113→325 para esta árvore **descartado** (medido, ver DONE). A rota de atualização, se um dia valer, é consertar o boot do `~/Android/not_samsung.sm8250-4.19` (4.19.325 + KSU), que já tem o source ACK e o mesmo suporte a dongle — o usuário já testou e **não boota no S20**. Diagnóstico barato disponível: `/proc/last_kmsg` (ramoops sec_debug, 2 MB, sobrevive ao reboot) aponta o culpado do boot falho. Custo: horas; se falhar não se perde nada (adicionado 2026-08-06, atualizado 2026-08-07)
- [ ] **Validar o OTG em 1500 mA com dongle plugado** — a v4.3 subiu o `OTG_ILIM` do max77705 de 900 para 1500 mA nos três pontos que escrevem o registrador. O teste é capturar em TX com o dongle e ver se some o `NOTIFY_EVENT_OVERCURRENT` do `dmesg` que derrubava o OTG. Depende do adaptador físico (mesmo bloqueio do item do monitor mode) (adicionado 2026-08-08)
- [ ] **App `com.matheus.charger`: baixar a frequência do polling de bateria** — o `ChargingNotificationService` (foreground service) varre `/sys/class/power_supply/battery/` **inteiro a 1,6 Hz**. Cada varredura dispara os `pr_info` do `sec_bat_show_attrs` e transações i2c ao MFC (que respondem `-6`, sem carregador wireless acoplado): 114 linhas/s de kmsg, que enchiam o ring buffer de 1 MB em **85 segundos**. Mitigado do lado do kernel na v4.4 (buffer para 4 MB), mas a correção real é no app: intervalo de 5–10 s e ler só os atributos usados, em vez do diretório todo (adicionado 2026-08-08)
- [ ] **Flashar a v4.5 e validar o `AGE_STEP` no aparelho** (2026-08-08) — o kernel e os zips estão prontos e publicados (raiz do repo + SD do S20), mas nada disso rodou no device ainda. Roteiro: (1) `battctl get AGE_STEP` deve dar `4` antes de mexer; (2) `battctl set AGE_STEP 0` e conferir se `batt_tune_float_voltage` sobe de 4270 para 4380; (3) no `dmesg`, a linha `BATT_AGE_STEP(0/4) float(4380mV)` — o `0/4` também confirma que `num_age_step` é 5, como a tabela do DTS indica; (4) step inválido (ex.: `7`) tem que **falhar** o write com `-EINVAL`, diferente do resto deste sysfs; (5) se a chave ficar no conf, conferir que o `service.sh` reaplica no boot. Se algo divergir, o `dmesg` agora tem ~5,5 min de janela em vez de 85 s (adicionado 2026-08-08)
- [ ] **(opcional, v4.4) Expor por patch em C o que só existe no device tree** — swelling completo (~24 props: `swelling_high_temp_block/recov/current/topoff`, `low_temp_1st/2nd/3rd`, `drop_float_voltage`), `recharge_condition_vcell/soc`, `full_condition_*`, as correntes `siop_*` e o `dc_step_chg_*` (25 W). Nada disso tem sysfs, e o DTB não chega ao device — só override no `sec_battery_dt.c`. Junto valeria `CONFIG_PD_CHARGER_HV_DISABLE=y`, sem o qual o `pd_disable` existe mas o handler inteiro está fora do build. Levantado em 2026-08-08, não decidido. **A v4.5 estabeleceu a receita** (o `batt_age_step`): enum em `sec_battery_sysfs.h` + `SEC_BATTERY_ATTR` na mesma posição relativa do array + `show`/`store`, e wrapper público quando a função alvo é `__visible_for_testing` — o custo por parâmetro agora é baixo (adicionado 2026-08-08)
