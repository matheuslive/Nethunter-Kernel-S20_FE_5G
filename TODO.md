# TODO

- [ ] **Usar dongle USB WiFi para monitor mode** — infraestrutura pronta (2026-08-07): drivers `=m`, firmwares em `packaging/firmware`, helper `usbwifi` (casa VID:PID→módulo) e `depmod` no companion. Falta apenas: (1) ~~instalar o companion~~ **feito em 2026-08-08** (v4.3, já com o fix de `system/bin`); (2) ter o adaptador físico (AR9271/ath9k_htc recomendado, RTL8812AU tb); (3) preparar helper de monitor+injection para o `wlan1`. Fluxo depois de plugar: `usbwifi` carrega o driver certo (adicionado 2026-08-06, atualizado 2026-08-07)
- [ ] **Apagar o `~/moncap` do device** (`/data/data/com.termux/files/home/moncap`) — o monitor mode no rádio interno trava o driver em D-state (hard reboot); o helper não deve ser usado (adicionado 2026-08-06)
- [ ] **Base do kernel — decisão parcial (2026-08-07):** fica no Nethunter 4.19.113 por ora (boota, em uso). Porte de stable 113→325 para esta árvore **descartado** (medido, ver DONE). A rota de atualização, se um dia valer, é consertar o boot do `~/Android/not_samsung.sm8250-4.19` (4.19.325 + KSU), que já tem o source ACK e o mesmo suporte a dongle — o usuário já testou e **não boota no S20**. Diagnóstico barato disponível: `/proc/last_kmsg` (ramoops sec_debug, 2 MB, sobrevive ao reboot) aponta o culpado do boot falho. Custo: horas; se falhar não se perde nada (adicionado 2026-08-06, atualizado 2026-08-07)
- [ ] **Validar o OTG em 1500 mA com dongle plugado** — a v4.3 subiu o `OTG_ILIM` do max77705 de 900 para 1500 mA nos três pontos que escrevem o registrador. O teste é capturar em TX com o dongle e ver se some o `NOTIFY_EVENT_OVERCURRENT` do `dmesg` que derrubava o OTG. Depende do adaptador físico (mesmo bloqueio do item do monitor mode) (adicionado 2026-08-08)
- [ ] **App `com.matheus.charger`: baixar a frequência do polling de bateria** — o `ChargingNotificationService` (foreground service) varre `/sys/class/power_supply/battery/` **inteiro a 1,6 Hz**. Cada varredura dispara os `pr_info` do `sec_bat_show_attrs` e transações i2c ao MFC (que respondem `-6`, sem carregador wireless acoplado): 114 linhas/s de kmsg, que enchiam o ring buffer de 1 MB em **85 segundos**. Mitigado do lado do kernel na v4.4 (buffer para 4 MB), mas a correção real é no app: intervalo de 5–10 s e ler só os atributos usados, em vez do diretório todo (adicionado 2026-08-08)
- [ ] **Medir a v4.6 contra a baseline (faltam so os dados)** (2026-08-10) — a v4.6 bootou e todos os pontos funcionais passaram (ver DONE); o que falta e **acumular 24-48 h de uso** e repetir a captura de `docs/baseline/` para comparar. Cuidados que o README da baseline detalha: (a) contadores do kernel zeram no boot e batterystats zera na carga — sao janelas diferentes, nao misturar; (b) a baseline foi tirada com a tela ligada **89,2% do tempo** (uso pesado, nao idle), entao so vale comparar contra periodo de uso parecido; (c) o numero mais direto de performance e a **razao system/user** do `Total cpu time` do batterystats — na v4.4 o UID 0 fechou `u=18m13s` contra `s=1h10m17s` em 3 h 22 min, e sao os debugs de hot path tirados na v4.6 que encarecem justamente esse `s`; (d) agora ha `time_in_state` do sistema inteiro, nao so o `uid_time_in_state` por UID
- [ ] **Decidir o que fazer com o piso do little (culpado ja identificado)** (2026-08-11) — e o `/vendor/bin/init.qcom.post_boot.sh`, bloco `"kona")`, linha 5233: `echo 691200 > .../policy0/scaling_min_freq`. **Nao e restricao de hardware** — escrever `300000` funciona, persiste (o script roda uma vez, no boot) e o little passa a usar 300/403/518/614 MHz na hora; medido ~4,5% de residencia nas 4 freqs baixas em ~20 min de uso leve. Trade-off: o 691200 e provavelmente deliberado da Qualcomm por latencia/jank na UI, entao remover troca energia por responsividade — nao e ganho gratis, precisa de teste de uso real. **Persistir exige `service.sh` do modulo Magisk** (roda depois do post_boot); mudar no kernel/defconfig nao adianta. **Decisao de 2026-08-11: nao mexer por ora** — o teste foi revertido e o aparelho esta no valor de fabrica (691200). Se um dia valer testar, o caminho e o `service.sh`, e o criterio de aceite tem que incluir jank/responsividade, nao so residencia de frequencia
- [ ] **Medir v4.6 x v4.7 depois de 24-48 h de uso** (2026-08-11) — a v4.7 esta validada e rodando (ver DONE); falta so acumular uso e repetir a captura para comparar com `docs/baseline/v4.6-*` e `v4.4-*`. Criterios ja definidos: (a) razao **system/user** do `Total cpu time` do batterystats, para carga parecida — e o numero mais direto do efeito dos debugs de hot path; (b) residencia de frequencia por `time_in_state` (existe desde a v4.6); (c) `compact_stall`/`compact_fail` e `pgmajfault` do `/proc/vmstat`, agora que o THP parou de tentar alocar a esmo. Cuidado de sempre: a baseline da v4.4 foi tirada com tela ligada 89,2% do tempo, entao so vale comparar contra uso semelhante
- [ ] **ThinLTO: testado e DESCARTADO — so retomar com benchmark no aparelho** (2026-08-12) — builda (exit 0), mas o `text` fica **+0,2%**, ou seja, o LTO praticamente nao mexeu no codigo; residente +0,7% e `Image.gz-dtb` +4,4%. E custa **quatro mudancas acopladas**: linker do kernel inteiro trocado para `ld.lld`, `MODVERSIONS` desligado (mudanca de ABI), `-j3` obrigatorio (a RAM da workstation e disputada com as VMs do PVE) e build muito mais lento. O ganho de "3-5%" **nunca foi verificado** aqui, e tamanho nao mede velocidade — se um dia for retomado, o criterio de aceite tem que ser **benchmark no aparelho**. Mapa completo dos obstaculos (incluindo o bug de `genksyms` com `union __packed` que existe na arvore desde sempre e vira erro fatal sob LTO) na memoria do projeto. Build descartado e arvore limpa; o `wirus_defconfig` nunca foi tocado
- [ ] **Dreno de standby — diagnostico FEITO e CORRIGIDO (2026-08-12)** — o consumo de tela apagada e alto (**63% em 8h59m ≈ 7%/h**, contra 1-2%/h saudavel) e vem de duas fontes, ambas de userspace, nenhuma delas defeito: (1) **Mi Band** por BLE/GATT, mantida pelo app `com.mc.miband1` — `hal_bluetooth_lock` 14,86 h e `qup_uart` 14,98 h de 35,1 h = **~42% do uptime**; e o preco de usar a pulseira, decisao do usuario; (2) **NFC**, que enquanto **ligado** segura o wakelock **~98% do tempo** (47 min de wakelock nos 48 min em que ficou ligado apos o boot) — ja esta desligado desde 2026-08-11 13:18, e o certo e manter assim fora do pagamento por aproximacao. Sobra a deteccao de atividade do Google, que virou item proprio abaixo. **O AOD fica como esta — decisao do usuario em 2026-08-13**, apesar dos 1092 wakeups; nao levantar de novo. ⚠️ Duas conclusoes minhas anteriores estavam erradas e foram derrubadas pelo usuario — os detalhes e as licoes de metodo estao na memoria do projeto
- [ ] **Desativar a deteccao de atividade do Google** (2026-08-13) — o `com.google.android.gms` e o maior gerador de wakeups por alarme: **1217 no total, dos quais 719 sao do `*walarm*:com.google.android.location.ALARM_WAKEUP_ACTIVITY_DETECTION`** (medido na v4.7, `dumpsys alarm`, 35 h de uptime). E o reconhecimento de atividade fisica (andando/correndo/dirigindo), que roda em background e acorda o aparelho periodicamente. Onde desligar: revogar a permissao **"Atividade fisica"** do Google Play Services (Configuracoes > Apps > Google Play Services > Permissoes), e/ou Configuracoes > Localizacao > Servicos de localizacao. **Verificar o efeito depois:** `dumpsys alarm | grep -A3 "Top Alarms"` — o `ALARM_WAKEUP_ACTIVITY_DETECTION` deve sumir do topo, e os wakeups do GMS devem cair de ~1217 para perto de 500. ⚠️ Conferir se algo que voce usa depende disso (contagem de passos do Google Fit, deteccao de direcao do Maps, automacoes do Bixby/Tasker que reajam a atividade) — se depender, o custo pode valer a pena. **Nada disso e kernel**
- [ ] **Decidir se o `AGE_STEP` fica em 0** (2026-08-10) — o mecanismo esta validado e funciona (ver DONE), mas foi **revertido para 4** ao fim do teste, que e onde a celula estava. Deixar em `0` sobe a float de 4270 para 4380 mV e devolve capacidade, ao custo de acelerar o desgaste de uma celula que ja tem ~1706 ciclos e esta no ultimo degrau de age forecast justamente por isso — e um trade-off capacidade x longevidade que so o usuario decide. Se ficar em 0, conferir que o `service.sh` do modulo de bateria reaplica no boot
- [ ] **(opcional, v4.4) Expor por patch em C o que só existe no device tree** — swelling completo (~24 props: `swelling_high_temp_block/recov/current/topoff`, `low_temp_1st/2nd/3rd`, `drop_float_voltage`), `recharge_condition_vcell/soc`, `full_condition_*`, as correntes `siop_*` e o `dc_step_chg_*` (25 W). Nada disso tem sysfs, e o DTB não chega ao device — só override no `sec_battery_dt.c`. Junto valeria `CONFIG_PD_CHARGER_HV_DISABLE=y`, sem o qual o `pd_disable` existe mas o handler inteiro está fora do build. Levantado em 2026-08-08, não decidido. **A v4.5 estabeleceu a receita** (o `batt_age_step`): enum em `sec_battery_sysfs.h` + `SEC_BATTERY_ATTR` na mesma posição relativa do array + `show`/`store`, e wrapper público quando a função alvo é `__visible_for_testing` — o custo por parâmetro agora é baixo (adicionado 2026-08-08)
